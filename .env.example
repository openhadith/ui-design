# Corpus (read-only). The dashboard never writes here.
NEXT_PUBLIC_API_URL=https://api.openhadith.org/api

# Dashboard's own database: workflow state, edits, audit, teams, permissions.
STUDIO_DATABASE_URL=postgresql://localhost:5432/hadith_studio

# The public reading site, for "view on the public site" links.
NEXT_PUBLIC_SITE_URL=http://localhost:3001
